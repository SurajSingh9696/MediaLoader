fastapi==0.115.6
uvicorn[standard]==0.32.1
yt-dlp>=2024.12.23
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20
imageio-ffmpeg>=0.5.1
aiofiles==24.1.0
httpx==0.28.1

# YouTube PO Token provider (requires bgutil Node.js server, started by start.sh)
bgutil-ytdlp-pot-provider

# YouTube OAuth2 authentication (bypasses cloud-IP blocks — recommended for Render)
# Setup: run locally: yt-dlp --username oauth2 --password "" <youtube_url>
# Then base64-encode the token.json and set as YOUTUBE_OAUTH2_TOKEN env var
yt-dlp-youtube-oauth2

# Fallback libraries for specific platforms
pytubefix>=6.10.0
instaloader>=4.10.0
# Direct InnerTube API: tries TV_EMBEDDED/ANDROID_MUSIC clients which are less
# aggressively blocked than web/android from cloud IPs. Zero-auth required.
innertube>=5.0.0
