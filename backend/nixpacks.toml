[phases.setup]
aptPkgs = ["ffmpeg", "python3-pip"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "pip install yt-dlp"
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
